@model IEnumerable<Gaming_Club.Models.Games>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GameName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GameName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GameGUID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GameGUID })
        </td>
    </tr>
}

</table>
